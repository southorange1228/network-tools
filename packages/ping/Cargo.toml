[package]
name = "orange-ping"
version = "0.0.0"
description = "a ping tool"
license = "ISC"
edition = "2018"
exclude = ["dist/index.node"]

[lib]
crate-type = ["cdylib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[dependencies.neon]
version = "0.10"
default-features = false
features = ["napi-6"]
